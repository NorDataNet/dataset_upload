{
    "name": "nordatanet/dataset_uplad",
    "type": "drupal-module",
    "description": "NIRD dataset upload module",
    "license": "GPL-2.0+",
    "scripts": {
        "sniff": "phpcs --colors -n --standard=Drupal --extensions=php,module,yml src",
        "snifferfix": "phpcbf --colors -n --standard=Drupal --extensions=php,module,yml src"
    },
"extra": {
    "drush": {
      "services": {
        "drush.services.yml": "^11"
      }
    }
  }
}
